<% subtitle = local_assigns[:subtitle].presence || (user.email if show_email) %>
<se-profile class="app-table-profile" name="<%= user.name %>" <%= tag.attributes(subtitle: subtitle) if subtitle %> initials="<%= profile_initials(user.name) %>" image="<%= profile_photo_url_for(user) %>"></se-profile>
