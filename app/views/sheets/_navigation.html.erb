  <se-nav-tabs class="app-page-navigation" label="Sheet navigation" value="<%= action_name == 'translations' ? 'translations' : (%w[settings update].include?(action_name) ? 'settings' : 'translations') %>" options="<%= [{id: 'translations', label: 'Translations', icon: 'languages', href: translations_project_sheet_path(@project,@sheet)}, (policy(@sheet).settings? ? {id: 'settings', label: 'Settings', icon: 'settings', href: settings_project_sheet_path(@project,@sheet)} : nil)].compact.to_json %>"><span data-actions class="app-export-action"><% if policy(@sheet).export? %><se-button text="Export" variant="secondary" data-open-modal="export-modal"></se-button><% end %></span></se-nav-tabs>
  <%= render "shared/export_button", sheet: @sheet %>
